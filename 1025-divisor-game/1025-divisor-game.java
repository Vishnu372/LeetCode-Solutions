class Solution {
    public boolean divisorGame(int n) {
        int x=n%2;
        if(x==1)
        {
            return false;
        }
        else
        {
            return true;
        }
    }
}